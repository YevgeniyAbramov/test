## Быстрый старт

### Запуск через Docker Compose

```bash
docker-compose up -d
```

Приложение будет доступно по адресу: `http://localhost:4001`

Swagger документация: `http://localhost:4001/swagger/index.html`

### Остановка

```bash
docker-compose down
```
